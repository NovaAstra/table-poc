export type ItemResize = Readonly<[index: number, size: number]>;

export type ItemsRange = Readonly<[startIndex: number, endIndex: number]>;

export type ItemSize = Readonly<[height: number, width: number]>